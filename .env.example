# Claude API
ANTHROPIC_API_KEY=

# 토스페이먼츠 키
# 테스트: https://developers.tosspayments.com > 테스트 API 키
# 실결제: https://developers.tosspayments.com > 라이브 API 키 (사업자 등록 후 발급)
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_gck_...    # 클라이언트 키 (공개)
TOSS_SECRET_KEY=test_gsk_...               # 시크릿 키 (비공개)
TOSS_WEBHOOK_SECRET=                       # 웹훅 시크릿 (토스 대시보드에서 설정, 선택)

# Vercel KV (Upstash Redis) — vercel.com > Storage > Create KV
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
REDIS_URL=

# 텔레그램 알림 (선택) — 결제 성공 시 알림
TELEGRAM_BOT_TOKEN=                        # BotFather에서 발급
TELEGRAM_CHAT_ID=                          # chat_id (숫자)
